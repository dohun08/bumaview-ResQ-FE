import styled from "styled-components";

export const CharacterImage = styled.img`
  position: absolute;  /* 부모 기준으로 위치 잡힘 */
  bottom: -10%;
  right: -30%;
  width: 200px;
  height: auto;
  z-index: 1000;
`;